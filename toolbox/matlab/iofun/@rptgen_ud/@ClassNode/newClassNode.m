function handleOut = newClassNode(parent, uddObj, canonFlag)
    % 1 15
    % 2 15
    % 3 15
    % 4 15
    % 5 15
    % 6 15
    % 7 15
    % 8 15
    % 9 15
    % 10 15
    % 11 15
    % 12 15
    % 13 15
    % 14 15
    handleOut = rptgen_ud.ClassNode;
    % 16 18
    % 17 18
    if gt(nargin, 1.0)
        handleOut.ClassPointer = uddObj;
        if gt(nargin, 2.0)
            handleOut.IsCanonical = canonFlag;
        end % if
    end % if
end % function
