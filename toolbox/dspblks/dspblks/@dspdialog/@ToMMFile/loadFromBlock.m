function loadFromBlock(h)
    % 1 8
    % 2 8
    % 3 8
    % 4 8
    % 5 8
    % 6 8
    % 7 8
    h.outputFilename = h.Block.outputFilename;
    h.streamSelection = h.Block.streamSelection;
    h.videoCompressor = h.Block.videoCompressor;
    h.audioCompressor = h.Block.audioCompressor;
    h.imagePorts = h.Block.imagePorts;
end % function
