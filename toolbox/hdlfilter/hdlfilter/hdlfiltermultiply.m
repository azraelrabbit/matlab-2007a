function [hdlbody, hdlsignals] = hdlfiltermultiply(in1, in2, out, rounding, saturation)
    % 1 9
    % 2 9
    % 3 9
    % 4 9
    % 5 9
    % 6 9
    % 7 9
    % 8 9
    [hdlbody, hdlsignals] = hdlmultiply(in1, in2, out, rounding, saturation);
end % function
