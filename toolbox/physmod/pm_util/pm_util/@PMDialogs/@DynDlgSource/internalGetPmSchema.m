function pm_schema = internalGetPmSchema(hThis, hBlk)
    % 1 8
    % 2 8
    % 3 8
    % 4 8
    % 5 8
    % 6 8
    % 7 8
    error('PMDialogs.DynDlgSource.internalGetPmSchema', 'This method must not be called from the base class.  Only the subclass''s overriden version can be invoked');
    % 9 11
    % 10 11
    pm_schema = [];
    % 12 13
end % function
